#!/usr/bin/env python3
"""Primary CLI entrypoint for Infrastructure Engineering Agent."""
from pathlib import Path
import os
import runpy
import sys

os.environ["INFRA_AGENT_ENTRYPOINT"] = "agent"
sys.argv[0] = "agent"
runpy.run_path(str(Path(__file__).with_name("harness")), run_name="__main__")
